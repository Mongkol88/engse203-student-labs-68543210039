# B1 · บันทึกการแก้บั๊ก 6 จุด — สอบกลางภาค ENGSE203

**ชื่อ–นามสกุล:** มงคล อาษากิจ **รหัส:** 68543210039-2 **Sec:** 1 **ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

กรอกให้ครบ **ทั้ง 6 บั๊ก** · แต่ละบั๊กเขียน 3 อย่างด้วย **คำของตัวเอง** (จะถูกสุ่มถามใน oral)

| คอลัมน์ | ใส่อะไร | ตัวอย่างสิ่งที่เขียน *(บั๊กสมมติ)* |
|---|---|---|
| **ไฟล์ : บรรทัด** | ไฟล์ที่แก้ + เลขบรรทัด | `Foo.jsx : 12` |
| **สาเหตุ** | **ทำไม** มันทำงานผิด (ไม่ใช่แค่ "พิมพ์ผิด") | "ใส่วงเล็บท้ายชื่อฟังก์ชัน เลยถูกเรียกทันทีตอน render" |
| **แก้อย่างไร** | เปลี่ยน**จากอะไร**เป็น**อะไร** | "เอาวงเล็บออก จาก `onClick={fn()}` เป็น `onClick={fn}`" |

> เคล็ด: เลขบรรทัดดูได้จากแถบซ้ายใน VS Code หรือจากข้อความ error ใน Console (เช่น `RequestList.jsx:9`)

---

## ตัวอย่างรูปแบบการเขียน

> ⚠️ นี่คือ **บั๊กสมมติเพื่อสอนวิธีเขียน** — **ไม่ใช่** บั๊กจริงในข้อสอบ

| อาการ | ไฟล์ : บรรทัด | สาเหตุ | แก้อย่างไร |
|---|---|---|---|
| กดปุ่มไม่ได้ แต่พอโหลดหน้าเสร็จฟังก์ชันทำงานเองทันที | `Foo.jsx : 12` | เขียน `onClick={handleClick()}` มีวงเล็บ → JavaScript **เรียกฟังก์ชันทันที**ตอน render แล้วเอาค่าที่ return มาเป็น handler | เอาวงเล็บออก เป็น `onClick={handleClick}` (ส่งตัวฟังก์ชัน ไม่ใช่ผลลัพธ์) |

---

## บันทึกของฉัน — กรอกทั้ง 6 บั๊ก

| # | อาการ (ให้มาแล้ว) | ไฟล์ : บรรทัด | สาเหตุ (ทำไมผิด) | แก้อย่างไร |
|---|---|---|---|---|
| 1 | Console เตือนสีเหลืองเรื่องรายการ (list) | RequestList.jsx : 8-9 | ใน RequestCard ไม่มี key  | ใส่ key={request.id} ใน RequestCard |
| 2 | ตัวเลข "รอดำเนินการ" ในแผงสรุปไม่ตรงกับที่เห็นจริง | DashboardPage : 54-55 | บรรทัด 54-55 เกิดจากการตั้ง request.status !== statusFilter  | บรรทัด 54-55 เเก้โโดยการตั้งการเปรียบเทียบจาก request.status !== statusFilter เป็น request.status === statusFilter); |
| 3 | กดตัวกรอง "รอดำเนินการ" แล้วได้รายการที่ไม่ใช่ | ashboardPage : 47  | บรรทัด 47 filler ถูกตั้งเป็น completed ซึ่งซ้ำกับบรรทัด 49 ไปเเล้ว | บรรทัด 47 เเก้โดยการเปลี่ยนจาก completed เป็น pending |
| 4 | เปลี่ยน URL `REQ-001` → `REQ-002` แล้วข้อมูลไม่เปลี่ยน | RequestDetailPage : 16-31 | เกิดจากการใส่ if ingore ใน .then เเละ .catch ถ้าเกิด ignore เป็น true ก็จะถูก return กลับทันทีทำให้ไม่ reload ตอนเปลี่ยน url  | ลบโค้ดที่เกี่ยวกับ ignore ให้หมดหรือเงื่อนไขเเละเพิ่ม   setErrorMessage(''); ด้วย |
| 5 | กด "ลบ" แล้วรายการยังอยู่ ต้องรีเฟรชถึงหาย | DashboardPage : 62-69  | เกิดจากการใส่  setRequests(requests); เป็น (requests) ทำให้มันถูกกดลบเเล้วไม่หายทันที | ทำให้การเปลี่ยนจาก (requests) เป็น (nextRequests) |
| 6 | กด "Reset Demo Data" แล้วหน้าพัง/ว่างเปล่า | DashboardPage : 75 | เกิดจากการ setRequests() ใส่ใน para เป็น resetRequests() เลยทำให้เมื่อกด Reset data เลยพัง | เพิ่ม const seedRequests = await resetRequests(); เเละ เปลี่ยน setRequests() ใน para เป็น seedRequests |

---

## เช็คก่อนส่ง

- [x] กรอกครบทั้ง 6 บั๊ก (ไฟล์:บรรทัด · สาเหตุ · แก้อย่างไร)
- [x] เขียนด้วยคำของตัวเอง ไม่ใช่ก๊อปคำอธิบายจาก AI มาตรง ๆ
- [x] ยืนยันแล้วว่าอาการทั้ง 6 หายจริงหลังแก้ (`npm run dev` แล้วลองทีละอาการ)
