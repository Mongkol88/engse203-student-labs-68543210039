# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นาย มงคล อาษากิจ **รหัส:** 68543210039-2 **Sec:** 1 **ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 11:05 | B1 | ทำไมใน handleDelete พอลบ try catch ออกทำให้พอลบ request ออกเว็บก็รีโหลดด้วยเลย | อธิบายว่าเกิดจาก Uncaught Error / ReferenceError ใน async function และ default behavior ของ form/button ที่ไม่มี preventDefault | แก้ไขโค้ด `handleDelete` ให้ใช้ `setRequests(nextRequests)` ที่ถูกต้อง และคง `try...catch` ไว้เพื่อดักจับ error · ทดสอบกดลบแล้ว state อัปเดตและเว็บไม่รีโหลด |
| 11:42 | B2.2 | react วิธีการกรองชื่อหรือกรองรายละเอียดใน `<input>` | แนะนำการใช้ Controlled Input เก็บ state คำค้นหา และใช้ `.filter()` ร่วมกับ `.includes()` และ `.toLowerCase()` | นำแนวคิดมาปรับใช้ โดยสร้าง state `search` และเขียน logic กรองข้อความลงในคอมโพเนนต์ |
| 11:56 | B2.2 | ทำให้การกรองไม่สนตัวเล็กหรือตัวใหญ่ยังไง | แนะนำการใช้ `.toLowerCase()` กับทั้งคำค้นหาและฟิลด์ข้อมูล หรือใช้ `RegExp` พร้อม flag `i` | เลือกใช้วิธี `.toLowerCase()` ร่วมกับ `.trim()` เพื่อตัดช่องว่างหน้า-หลังก่อนนำไปค้นหา |
| 12:07 | B2.2 | รองรับการกรองภาษาไทยไหม และต้องการกรองแค่ชื่อและคำอธิบาย | ยืนยันว่า `.includes()` รองรับภาษาไทยได้ และแนะนำให้ใช้ `.normalize('NFC')` พร้อมปรับฟิลด์ให้ตรวจเฉพาะชื่อและคำอธิบาย | ปรับเงื่อนไขใน `filteredRequests` ให้ตรวจเฉพาะ `request.requesterName` และ `request.details` พร้อมใส่ `.normalize('NFC')` เพื่อความแม่นยำ |
| 12:15 | B2.3 | วิธีการกรอง radio | อธิบายการใช้ State ตัวเดียวสำหรับ Radio Group ที่มี `name` เดียวกัน และตรวจเงื่อนไข `status === selectedStatus` | ทำความเข้าใจการทำงานของ Controlled Radio เพื่อนำมาใช้ตรวจสอบกับการทำงานของ `FilterBar` |
| 12:43 | B2.3 | ส่งโค้ด DashboardPage ให้ตรวจว่าทำไมยังไม่กรองสถานะ | ชี้จุดผิดในโค้ด: `matchStatus` ขาดเงื่อนไข `| request.status === statusFilter` และมีเครื่องหมาย `;` คั่นกลางทำให้โค้ดบรรทัดล่างไม่ทำงาน  แก้ไขบรรทัด `matchStatus` ให้เป็น `statusFilter === 'all'  request.status === statusFilter` และลบ `;` ที่เกินออก · ทดสอบกดเปลี่ยนตัวกรองสถานะแล้วข้อมูลแสดงผลถูกต้อง | 
| 12:55 | B2.3 | วิธีการกรองร่วมกับการกรองสถานะ เช่น "completed ห้อง" | แนะนำการแยกคำค้นหาด้วย `.split(/\s+/)` แล้วใช้ `.every()` ตรวจสอบข้อความรวม (`fullText`) เพื่อรองรับการค้นหาหลายคำพร้อมกัน | นำ logic tokenized search มาปรับใช้ ทำให้สามารถพิมพ์ค้นหาสถานะพร้อมกับคำค้นหาในช่องเดียวกันได้ · ทดสอบพิมพ์ "completed ห้อง" แล้วได้ผลลัพธ์ตรง |
| 13:05 | B2 | เมื่อกรองแล้วไม่มีผลลัพธ์ ต้องแสดงข้อความแบบไหน | แนะนำให้ตรวจ `filteredRequests.length === 0` แล้วแสดงข้อความที่เหมาะสม เช่น `ไม่พบคำร้องที่ตรงกับการค้นหา` | เพิ่มเบรคใน `RequestList` ให้ return `<p>` เมื่อไม่มีข้อมูลและเช็กว่า UI ยังคงแสดงผลถูกต้องเมื่อกรองไม่เจอ |
| 13:16 | B3.2 | ปุ่มทำเสร็จใน RequestDetailPage ทำไมยังไม่เปลี่ยน status | AI ชี้ว่า `updateRequestStatus` ยังไม่ถูก import และ function ถูกเรียกแต่ undefined | เพิ่ม `import { getRequestById, updateRequestStatus } from '../services/requestService.js'` แล้วเรียก `handleMarkDone` ให้ตั้งค่า state ใหม่หลังอัปเดตเสร็จ · ทดสอบกดปุ่มแล้วสถานะเปลี่ยนเป็น `completed` |
| 13:25 | B3.1 | ถ้าต้องการปุ่มทำเสร็จแสดงเฉพาะเมื่อยังไม่ completed ต้องเขียนอย่างไร | แนะนำใช้เงื่อนไข `request.status !== 'completed' && <button ...>` | ปรับ JSX ให้แสดงปุ่มได้ก็ต่อเมื่อ status ไม่ใช่ completed และซ่อนหาก已เสร็จแล้ว |
| 13:46 | B4.1 | PriorityBadge คืออะไร และควรใช้ยังไง | อธิบายว่าเป็น component สำหรับแสดงระดับความสำคัญของคำร้อง เช่น `normal`, `urgent` และแยก UI ออกจาก RequestCard | นำ `PriorityBadge` มาใช้แทนการแสดง `request.priority` ตรง ๆ และให้แยก styles ออกจาก card เพื่อให้ code อ่านง่ายขึ้น |
| 14:11 | B4.1 | ถ้า priority ไม่มีค่า หรือค่าไม่รู้จัก ให้แสดงอย่างไร | แนะนำ normalize ให้เป็น `unknown` แล้ว map เป็นข้อความ `Unknown` | เพิ่มเงื่อนไข `['normal', 'urgent'].includes(priority) ? priority : 'unknown'` และออกแบบ fallback เพื่อไม่ให้ UI หลุดหรือ error |

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** นาย มงคล อาษากิจ